{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "AccountHub 默认权限：核心 event + opener 外链",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
